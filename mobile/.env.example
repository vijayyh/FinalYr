# Copy to .env and set to override the API base URL (e.g. for local backend development).
# If unset, the app defaults to the deployed backend (resumepro-backend-cthc.onrender.com)
# so the phone and dev machine never need to share a Wi-Fi network.
# EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
